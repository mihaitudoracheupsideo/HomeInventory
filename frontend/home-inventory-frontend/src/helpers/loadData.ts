/*import type { DataType } from '../types/Enums'

export const loadItems = async (dataType: DataType, setter: unknown[]) => {
    try {
      const res = await getItems();
      setter(res.data);
    } catch (err) {
      console.error("Error fetching objects", err);
    }
  };
  */